# Copiez ce fichier en .env (gitignoré) pour le développement local.
# Les mêmes variables doivent être configurées sur Netlify
# (Site config -> Environment variables).

# Token personnel Airtable (scopes : data.records:read/write, schema.bases:read)
AIRTABLE_API_KEY=pat_votre_token_personnel_airtable

# Base DEDIEE "Visites techniques INSTANT BY PINTO"
AIRTABLE_BASE_ID=appiH3g9J0B9tdbIE

# Base maintenance "Abonnements Entretien & Ramonage" — lecture seule
# (recherche client pour pré-remplir une visite). Optionnel.
AIRTABLE_CLIENTS_BASE_ID=appCSUjH1ht16bL97

# PIN global de secours (utilisé si aucun technicien ne correspond)
APP_PIN=changez_moi

# Secret de signature des tokens de session (>= 32 caractères aléatoires)
APP_SECRET=generez_une_chaine_aleatoire_de_32_caracteres_minimum
